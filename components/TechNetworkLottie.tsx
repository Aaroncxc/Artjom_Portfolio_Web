'use client';

import { DotLottieReact } from '@lottiefiles/dotlottie-react';

/**
 * Bottom-band constellation loop (custom Atelier Noire build).
 * Regenerated via scripts/build-tech-network-lottie.mjs → /bg/tech-network.json
 * Kill switch: remove mount in LightLeaksBackground.
 */
export function TechNetworkLottie() {
  return (
    <DotLottieReact
      src="/bg/tech-network.json"
      loop
      autoplay
      className="h-full w-full"
    />
  );
}
